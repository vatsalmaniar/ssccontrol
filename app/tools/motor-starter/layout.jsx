export const metadata = {
  title: 'Motor Starter Selection Tool',
  description: 'Free tool: find the correct contactor, overload relay and SCPD combination for your motor (IEC Type 2 coordination).',
};
export default function Layout({ children }) {
  return children;
}
